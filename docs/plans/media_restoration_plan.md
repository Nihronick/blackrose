# Plan: Восстановление медиа

## Шаг 1: Оптимизация фронтенда
- Изменить `apiGetProxyUrl` в `frontend/src/lib/api.ts`, чтобы функция проксировала ТОЛЬКО ссылки, содержащие `attachments`.
- Все остальные ссылки (эмодзи, иконки) возвращать как есть.

## Шаг 2: Оптимизация бэкенда
- Заменить `requests.head` на асинхронный вызов через `httpx` (нужно добавить в `requirements.txt` если нет, или использовать `aiohttp`).
- В `admin_media_proxy` (файл `backend/routers/admin.py`) реализовать получение `Content-Length` асинхронно.

## Шаг 3: Проверка
- Запустить синтез в Discord Lab.
- Проверить, что эмодзи грузятся мгновенно (напрямую).
- Проверить, что видео грузится через прокси и имеет длительность.

## Шаг 4: Обновление документации
- Обновить `CLAUDE.md`.
- Обновить `todo.md`.
