feat: FTS search, audit log, history, reorder, export/import, inline bot

backend:
- database.py: tsvector + GIN index для полнотекстового поиска (Russian),
  fallback на ILIKE; auto-trigger на INSERT/UPDATE; backfill существующих строк
- database.py: таблица guide_history — аудит create/update/delete/import
  с snapshot и user id
- database.py: preview (первые 80 символов без HTML) + has_photo/video/document
  в get_guides_by_category
- database.py: reorder_guides / reorder_categories — drag-and-drop сортировка
- database.py: export_all / import_guides — JSON backup/restore
- main.py: новые эндпоинты /api/admin/reorder/guides|categories
- main.py: /api/admin/guide/{key}/history — лог изменений гайда
- main.py: /api/admin/export, /api/admin/import
- main.py: /api/category/{key} теперь возвращает preview + has_photo/video/document
- main.py: changed_by (user_id) пишется в audit log при save/delete через admin

bot:
- handlers/miniapp.py: reply-кнопка «📖 Открыть гайды» теперь НЕ открывает
  WebApp напрямую (reply + WebAppInfo не передаёт initData — ограничение Telegram);
  вместо этого бот присылает inline-кнопку в ответ — так initData передаётся корректно
- handlers/miniapp.py: /start — inline-кнопка для всех пользователей, не только админов
- handlers/miniapp.py: inline-режим — поиск гайдов из любого чата (@bot запрос)
- handlers/miniapp.py: /search <запрос> — поиск без открытия приложения
- handlers/miniapp.py: deep links — /start guide_<key> открывает конкретный гайд
- config.py: добавлена переменная API_URL (нужна боту для поиска)
- requirements.txt: добавлен aiohttp

frontend:
- App.jsx: исправлен баг — onOpenGuide не передавался в GuideView,
  из-за чего «Открыть гайд» в cyberlink popup не работал
- App.jsx: поддержка deep link ?guide=key при загрузке
- hooks/useHistory.js: история просмотров в Telegram CloudStorage (последние 20)
- views/HistoryView.jsx: экран «Недавние»
- views/GuideView.jsx: callback onGuideLoaded — уведомляет App о загруженном гайде
  для корректного добавления в историю с title + icon
- components/ReorderList.jsx: drag-and-drop сортировка через HTML5 Drag API
- components/ExportImport.jsx: скачать/загрузить JSON прямо из вкладки «Экспорт»
- views/AdminView.jsx: новая вкладка «Экспорт», drag-and-drop в списке гайдов
- api.js: apiPost, apiReorderGuides, apiReorderCategories, apiExport, apiImport,
  apiGuideHistory
